# Pull Requests

- Target `origin` (`gsong/eyebreak`) for every PR.
- Never open a PR against `upstream` (`cheat2001/eyebreak`) unless George explicitly says so.
- When he does, branch from `upstream/main`, never from `main` — our `main` carries fork-only
  tooling that would land in the PR. Use the `upstream-pr` skill for the full workflow.
