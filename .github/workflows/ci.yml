name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

# Builds, tests and lints only; nothing here writes to the repo.
permissions:
  contents: read

jobs:
  build:
    name: Build and Test
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Nothing here pushes with the token; drop it from .git/config.
          persist-credentials: false

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Build
        run: |
          xcodebuild clean build \
            -project EyeBreak.xcodeproj \
            -scheme EyeBreak \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      # This step gates the build. It has no `|| echo` fallback and no
      # continue-on-error, so a failing test fails CI.
      - name: Run tests
        run: |
          xcodebuild test \
            -project EyeBreak.xcodeproj \
            -scheme EyeBreak \
            -destination 'platform=macOS' \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            DEVELOPMENT_TEAM=""

      - name: Check for warnings
        run: |
          xcodebuild clean build \
            -project EyeBreak.xcodeproj \
            -scheme EyeBreak \
            -configuration Debug \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            | grep -A 5 "warning:" || echo "No warnings found! 🎉"
        continue-on-error: true

  lint:
    name: SwiftLint
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Nothing here pushes with the token; drop it from .git/config.
          persist-credentials: false

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: swiftlint lint --reporter github-actions-logging
        continue-on-error: true

  code-quality:
    name: Code Quality Checks
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Nothing here pushes with the token; drop it from .git/config.
          persist-credentials: false

      - name: Check for TODO/FIXME
        run: |
          echo "Checking for TODO/FIXME comments..."
          grep -r "TODO\|FIXME" EyeBreak/ || echo "No TODO/FIXME found"
        continue-on-error: true

      - name: Check file permissions
        run: |
          find . -name "*.sh" -type f -exec test -x {} \; -exec echo "✓ {}" \; || echo "Some shell scripts are not executable"
